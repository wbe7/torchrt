apiVersion: v1
kind: Service
metadata:
  name: {{ include "torchrt.fullname" . }}
  labels:
    {{- include "torchrt.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "torchrt.selectorLabels" . | nindent 4 }}